image: docker:stable

stages:
    - build
    - test
    - deploy

pullCode:
    stage: build
    script:
        - echo "Building"
        - mkdir build
        - touch build/info.txt
    artifacts:
        paths:
            - build/

buildImages:
    stage: build
    script:
        - echo "Building"
        - mkdir build
        - touch build/info.txt
    artifacts:
        paths:
            - build/

testFrontStatic:
    stage: test
    script:
        - echo "Test"
        - test -f "build/info.txt"

testFrontPHP:
    stage: test
    script:
        - echo "Test"
        - test -f "build/info.txt"

deploy:
    stage: deploy
    only:
        - master
    script:
        - echo "deployed system"
